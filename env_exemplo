#Sempre replicar esse arquivo e nomear como .env e colocar nele a senha do banco
# o shart 1 no APP_AUTH_SECRET.
NODE_ENV=development
#NODE_ENV=production

APP_DB_HOST=localhost
APP_DB_PORT=3306
APP_DB_NAME=projeto-final
APP_DB_USER=root
APP_DB_PASSWORD=

APP_AUTH_SECRET=escolher um segredo